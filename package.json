{
  "name": "vmw-kanupolo-live",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:parse": "node scripts/runLocal.mjs",
    "preview:data": "node scripts/buildLocalSnapshot.mjs",
    "preview": "npm run preview:data && node scripts/serveLocal.mjs"
  },
  "dependencies": {
    "cheerio": "^1.2.0"
  },
  "devDependencies": {
    "@netlify/blobs": "^7.4.0",
    "@netlify/functions": "^2.8.0"
  }
}
